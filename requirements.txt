# Bringh Bot - Requirements
# ==========================

# Zalo Bot
python-zalo-bot==0.1.9

# HTTP Client
httpx>=0.25.0

# Environment variables
python-dotenv>=1.0.0

# Flask for admin web
Flask>=3.0.0
Flask-SocketIO>=5.3.0

# OCR (Optional - choose one)
# EasyOCR (recommended - offline)
easyocr>=1.7.0

# Tesseract OCR (alternative - requires system installation)
# pytesseract>=0.3.10
# pillow>=10.0.0

# Database (SQLite is built-in, but we use these for convenience)
# aiosqlite>=0.19.0

# For async support
# asyncio is built-in

# For better JSON handling
orjson>=3.9.0

# For type hints (optional)
typing-extensions>=4.8.0
